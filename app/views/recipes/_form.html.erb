<div class="form-horizontal" role="form">
  <%= form_for recipe do |f| %>
    <div class="form-group">
      <%= f.label :name, class: 'col-sm-2 control-label' %>
      <div class="col-sm-4">
        <%= f.text_field :name, class: 'form-control', placeholder: "Enter recipe name" %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :description, class: 'col-sm-2 control-label' %>
      <div class="col-sm-8">
        <%= f.text_area :description, rows: 4, class: 'form-control', placeholder: "Enter recipe description" %>
      </div>
    </div>
    <div class="form-group">
      <%= f.label :Ingredient, class: 'col-sm-2 control-label' %>
    </div>

    <div class="add-ln-form">
      <%= f.fields_for :ingredient_lines do |ingredient_form| %>
        <%= render 'ingredient_line_fields', f: ingredient_form %>
      <% end %>
    </div>
    <div class="add-ln-form">
      <div>
        <%= link_to_add_fields '<div class="btn btn-primary btn-lg center-block">Add</div>'.html_safe, f, :ingredient_lines %></a>
      </div>
    </div>
  </div>
    <div class="form-group">
      <%= f.submit "Save", class: 'btn btn-success' %>
    </div>  
  <% end %>
</div>

